Problem:
We need to gain access to some routers. Let's try and see if we can find the password in the captured network data: data.pcap.

Explanation:
.pcap
A Packet Capture (pcap) consists of an API for capturing network traffic 

Wireshark
Sniffing & Spoofing tool that allows you to read .pcap files

Base64
A type of encoding that typically ends with ==
